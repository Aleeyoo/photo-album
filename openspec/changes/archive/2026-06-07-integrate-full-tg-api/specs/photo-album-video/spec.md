## MODIFIED Requirements

### Requirement: Video media items show thumbnail in grid
REPLACED. Grid cards for video use `poster` URL as thumbnail image instead of dark placeholder. Play icon overlay is kept. Fallback to dark placeholder when no poster.
